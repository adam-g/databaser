/*
* Det här är Adams fulkopierade fil med kod från testfile,
* det är de 6 raderna efter att den här kommentaren tar slut. 
*/

create table test (id int, name varchar(20));

insert into test values (157, 'kalle');

select * from test;